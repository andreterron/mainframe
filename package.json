{
    "name": "root",
    "author": "Andre Terron (https://andreterron.com)",
    "private": true,
    "license": "MIT",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "yarn workspace @mainframe-so/webapp run dev",
        "build": "yarn workspace @mainframe-so/webapp run build",
        "start": "node scripts/generate-env.mjs && yarn workspace @mainframe-so/webapp run dev"
    },
    "devDependencies": {
        "concurrently": "^8.2.0",
        "nanoid": "^4.0.2"
    }
}
